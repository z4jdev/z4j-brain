# Changelog

## 1.4.0 (2026-05-02)

Compatibility shim. `pip install z4j` now resolves to this metadata-only release that declares a single dependency on `z4j>=1.4.0`. The brain server source moved into the `z4j` distribution. Frozen at 1.4.0; new features land in `z4j`.
